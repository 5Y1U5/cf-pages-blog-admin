-- 本人にパスワードを変えてもらう必要があるかどうか。
-- 管理者が発行した初期パスワード・再発行パスワードは、渡す途中（チャットやメール）に
-- 平文が残るため、受け取った本人が自分のパスワードに変えるまでこのフラグを立てておく。
--
-- 既存ユーザーは 0（既定）のまま。稼働中のサイトで全員がいきなり変更を求められることは無い。
ALTER TABLE users ADD COLUMN must_change_password INTEGER NOT NULL DEFAULT 0;
